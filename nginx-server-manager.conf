# Nginx configuration for server manager
# Add this to your existing Nginx site configuration

location /server-manager {
    alias /root/bisnesgpt-server/public;
    try_files /server-manager.html =404;
}

location /api/server-manager/ {
    proxy_pass http://localhost:9000/api/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
}
